language: objective-c

before_install: 
- brew update
- if brew outdated | grep -qx xctool; then brew upgrade xctool; fi

script: 
- xctool clean build -project PxDensity.xcodeproj -scheme PxDensity
- xctool test -project PxDensity.xcodeproj -scheme PxDensityTests
